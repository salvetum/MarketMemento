/*
 * React is bootstrapped from src/main.jsx.
 *
 * This file is intentionally retained as a compatibility path for links and
 * older deployments that referenced app.js directly. UI state, event wiring,
 * persistence and rendering now live in src/App.jsx and src/services/.
 */
